package org.firstinspires.ftc.teamcode.pedro;

public class Tuning {
    /*
     * Tuners go here.
     *
     * Follow the tuning guide at https://pedropathing.com/ and add @Tuner methods
     * for the procedures you want to run. Everywhere the guide shows a HardwareMap,
     * use a SafeHardwareMap instead — Safe Pedro localizers/drivetrains are built
     * from it so all device access runs on Synapse's hardware thread.
     *
     * For example, once you have tuned constants in Constants.java:
     *
     * @Tuner
     * public static Procedure tests() {
     *     return new Tests(
     *             safeMap -> new Mecanum(safeMap, Constants.drivetrainConfig),
     *             safeMap -> new PinpointLocalizer(safeMap, Constants.localizerConfig),
     *             () -> new Foresight(Constants.foresightConfig));
     * }
     */
}
